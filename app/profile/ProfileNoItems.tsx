import { Stack, StackProps } from "@mantine/core"

const ProfileNoItems = (props: StackProps) => (
  <Stack align="center" justify="center" spacing={0} sx={({}) => ({ height: "100%" })} {...props} />
)

export default ProfileNoItems
